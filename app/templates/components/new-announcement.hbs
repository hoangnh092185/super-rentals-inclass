{{#if addNewAnnouncement}}
<h1>New Announcement</h1>
<div>
  <form >
    <div class="form-group">
      <label for="text">Add new Announcement</label>
      {{input value=text id="text"}}
    </div>
    <button {{action 'saveAnnouncement1'}}>Save</button>
  </form>
</div>
{{else}}
<button class="big-button" {{action 'announcementFormShow'}}>New Announcement</button>
{{/if}}
